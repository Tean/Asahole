spring:
  application:
    name: explosed_service #@configserveice: http://xx/{application}/profile
  cloud:
    config:
      uri: http://localhost:8901/
      label: master
      profile: expdev2 #@configserveice: http://xx/application/{profile}
    zookeeper:
      discovery:
        root: /spring-cloud-service